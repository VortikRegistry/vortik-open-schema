{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/VortikRegistry/vortik-open-schema/main/schemas/sequencingmarket/0.1-research/schema.json",
  "title": "Vortik Open Schema — Sequencing Markets",
  "description": "Machine-readable schema for the Sequencing Markets semantic anchor within the Vortik Semantic Registry. Research artifact; not an official protocol specification.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "version",
    "canonical_term",
    "classification",
    "status",
    "type",
    "summary",
    "pipeline_position",
    "coordination_role",
    "protocol_grounding",
    "naming",
    "sources"
  ],
  "properties": {
    "id": {
      "type": "string",
      "const": "sequencingmarket",
      "description": "Registry identifier for this semantic anchor."
    },
    "version": {
      "type": "string",
      "const": "0.1-research",
      "description": "Schema version for this anchor."
    },
    "canonical_term": {
      "type": "string",
      "const": "sequencing markets",
      "description": "Canonical term tracked by this external coordination surface."
    },
    "classification": {
      "type": "string",
      "const": "external",
      "description": "Semantic classification within the Vortik registry."
    },
    "status": {
      "type": "string",
      "const": "research",
      "description": "Current maturity/status of the tracked surface."
    },
    "type": {
      "type": "string",
      "const": "coordination_surface",
      "description": "External coordination surface related to transaction ordering, rollup sequencing, and sequencing infrastructure."
    },
    "summary": {
      "type": "string",
      "description": "External sequencing coordination surface across rollups, shared sequencing systems, transaction ordering infrastructure, and sequencing-related execution environments.",
      "minLength": 20
    },
    "pipeline_position": {
      "type": "array",
      "description": "Interpretive coordination domains where sequencing markets may be relevant.",
      "minItems": 1,
      "maxItems": 2,
      "items": {
        "type": "string",
        "enum": [
          "sequencing coordination",
          "transaction ordering"
        ]
      }
    },
    "coordination_role": {
      "type": "array",
      "description": "Key coordination functions performed by this external sequencing surface.",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 5
      }
    },
    "protocol_grounding": {
      "type": "array",
      "description": "Primary protocol, research, or ecosystem references grounding this external sequencing surface.",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 5
      }
    },
    "naming": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "ens",
        "canonical",
        "mismatch",
        "notes"
      ],
      "properties": {
        "ens": {
          "type": "string",
          "const": "sequencingmarket.eth"
        },
        "canonical": {
          "type": "string",
          "const": "sequencing markets"
        },
        "mismatch": {
          "type": "boolean",
          "const": false
        },
        "notes": {
          "type": "string",
          "minLength": 10
        }
      }
    },
    "sources": {
      "type": "array",
      "description": "Primary source references for this anchor.",
      "minItems": 1,
      "items": {
        "type": "string",
        "format": "uri"
      }
    }
  }
}
