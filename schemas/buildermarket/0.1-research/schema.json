{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/VortikRegistry/vortik-open-schema/main/schemas/buildermarket/0.1-research/schema.json",
  "title": "Vortik Open Schema — Builder Markets",
  "description": "Machine-readable schema for the Builder Markets semantic anchor within the Vortik Semantic Registry. Research artefact; not an official protocol specification.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "version",
    "canonical_term",
    "classification",
    "status",
    "type",
    "summary",
    "pipeline_position",
    "coordination_role",
    "protocol_grounding",
    "naming",
    "sources"
  ],
  "properties": {
    "id": {
      "type": "string",
      "const": "buildermarket",
      "description": "Registry identifier for this semantic anchor."
    },
    "version": {
      "type": "string",
      "const": "0.1-research",
      "description": "Schema version for this anchor."
    },
    "canonical_term": {
      "type": "string",
      "const": "builder markets",
      "description": "Canonical technical term tracked by this anchor."
    },
    "classification": {
      "type": "string",
      "const": "core",
      "description": "Semantic classification within the Vortik registry."
    },
    "status": {
      "type": "string",
      "const": "research",
      "description": "Current maturity/status of the tracked surface."
    },
    "type": {
      "type": "string",
      "const": "market",
      "description": "Structural type of this coordination surface."
    },
    "summary": {
      "type": "string",
      "minLength": 20,
      "description": "Short description of builder market coordination."
    },
    "pipeline_position": {
      "type": "array",
      "description": "Pipeline stage where builder markets operate.",
      "minItems": 1,
      "items": {
        "type": "string",
        "enum": [
          "order flow",
          "visibility",
          "builder coordination",
          "inclusion",
          "execution",
          "finality"
        ]
      }
    },
    "coordination_role": {
      "type": "array",
      "description": "Key coordination roles of builder markets.",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 3
      }
    },
    "protocol_grounding": {
      "type": "array",
      "description": "Primary protocol or research references grounding builder markets.",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 3
      }
    },
    "naming": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "ens",
        "canonical",
        "mismatch",
        "notes"
      ],
      "properties": {
        "ens": {
          "type": "string",
          "const": "buildermarket.eth"
        },
        "canonical": {
          "type": "string",
          "const": "builder markets"
        },
        "mismatch": {
          "type": "boolean",
          "const": false
        },
        "notes": {
          "type": "string",
          "minLength": 5
        }
      }
    },
    "sources": {
      "type": "array",
      "description": "Primary source references for this anchor.",
      "minItems": 1,
      "items": {
        "type": "string",
        "format": "uri"
      }
    }
  }
}
