
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/VortikRegistry/vortik-open-schema/main/schemas/buildermarket/0.1-research/schema.json",
  "title": "Vortik Open Schema — Builder Market Semantic Anchor",
  "description": "Minimal metadata schema for builder market coordination terminology. Research artefact; not an official specification.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "protocol",
    "version",
    "identifier",
    "capabilities",
    "stewardship_status",
    "primitive_scope",
    "canonical_terms",
    "last_reviewed",
    "references"
  ],
  "properties": {
    "protocol": {
      "type": "string",
      "const": "BuilderMarket",
      "description": "Semantic anchor label for builder market coordination surfaces (non-normative)."
    },
    "version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+(-[a-z0-9.-]+)?$",
      "default": "0.1-research",
      "description": "Schema version (semver-ish)."
    },
    "identifier": {
      "type": "string",
      "const": "buildermarket.eth",
      "description": "ENS identifier mapped to this semantic anchor."
    },
    "primitive_scope": {
      "type": "string",
      "minLength": 20,
      "description": "Scope definition: market structures around block builders, bid discovery and builder competition; excludes generic MEV terminology not specific to builder markets."
    },
    "canonical_terms": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 2 },
      "description": "Terms observed in primary sources for this concept surface."
    },
    "capabilities": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": [
          "block-building-competition",
          "bid-discovery",
          "mev-supply-chain",
          "stewardship-metadata"
        ]
      },
      "description": "Declared semantic capability tags (non-normative)."
    },
    "stewardship_status": {
      "type": "string",
      "enum": [
        "protocol_unanchored",
        "alignment_pending",
        "institutionally_reserved",
        "transferred"
      ],
      "default": "alignment_pending",
      "description": "Registry stewardship status label (non-commercial)."
    },
    "terminology_evolution": {
      "type": "array",
      "description": "Non-normative timeline documenting terminology drift for this primitive surface.",
      "default": [],
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["term", "status"],
        "properties": {
          "term": { "type": "string", "minLength": 2 },
          "status": { "type": "string", "enum": ["canonical", "alias", "deprecated"] },
          "notes": { "type": "string", "maxLength": 280 },
          "first_seen": { "type": "string", "format": "date" },
          "last_seen": { "type": "string", "format": "date" }
        }
      }
    },
    "last_reviewed": {
      "type": "string",
      "format": "date",
      "default": "2026-03-11",
      "description": "Last review date for this schema artefact (YYYY-MM-DD)."
    },
    "sources_md": {
      "type": "string",
      "format": "uri",
      "default": "https://raw.githubusercontent.com/VortikRegistry/vortik-open-schema/main/schemas/buildermarket/0.1-research/sources.md",
      "description": "Pointer to curated sources file (Markdown)."
    },
    "references": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "format": "uri" },
      "description": "Primary-source references (URIs)."
    },
    "notes": {
      "type": "string",
      "maxLength": 800,
      "description": "Optional notes for ambiguity, scope boundaries or naming collisions."
    }
  }
}
